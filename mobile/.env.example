# Optional. Leave unset in Expo Go: the app derives the API address from the
# Expo dev server (same laptop, port 8080). Set this for a hosted API or when
# the phone is not on the same network as the laptop.
# EXPO_PUBLIC_API_URL=http://192.168.1.20:8080
